language: python

python:
    - "3.6"

install:
    - python3 setup.py install --user

script:
    - pip install pipenv
    - pipenv install --dev
    - pipenv install -e .
    - pipenv run flake8
    - pipenv run mypy alfred
    - pipenv run pytest
