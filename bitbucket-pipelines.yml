image: python:3.6

pipelines:
  default:
    - step:
        caches:
          - pip
        script:
          - pip install pipenv
          - pipenv install --dev
          - pipenv install -e .
          - pipenv run flake8
          - pipenv run mypy alfred
          - pipenv run pytest
